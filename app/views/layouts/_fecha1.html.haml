= text_field_tag 'fin','', :class=>'datepicker input'

:javascript
	$('.datepicker').datepicker({
	"format": "dd/mm/yyyy",
	"weekStart": 1,
	"autoclose": true,
	"language": "es"});

	$('#fin').val(fechaAhora());